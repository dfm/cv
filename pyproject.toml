[project]
name = "cv"
version = "0.0.0"
description = "Scripts for building Dan Foreman-Mackey's CV"
requires-python = ">=3.13"
dependencies = [
    "ads",
    "pypandoc-binary>=1.17",
    "requests",
]
